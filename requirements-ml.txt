# 경로 B: ML/DL 전략 전용 의존성
# 기존 requirements.txt와 분리 — 학습 환경에서만 전체 설치
# 라이브 서버에는 사용 모델 패키지만 선택 설치

# Phase B-1: ML (Gradient Boosted Trees)
lightgbm>=4.0.0
xgboost>=2.0.0
scikit-learn>=1.4.0

# Phase B-2: DL (LSTM / Transformer)
torch>=2.2.0

# Phase B-3: RL (PPO)
stable-baselines3[extra]>=2.3.0
gymnasium>=0.29.0

# 공통
pyarrow>=15.0.0        # parquet 피처 캐시
joblib>=1.3.0          # scaler 직렬화
