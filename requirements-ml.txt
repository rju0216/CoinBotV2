# ML/DL/RL 전략용 선택 의존성 (코어 requirements.txt 와 분리).
# 새 모델이 사용하는 패키지만 선택 설치하면 된다. 모든 패키지가
# 필요한 것은 아니다 — 모델 종류에 맞춰 취사선택.

# Gradient Boosted Trees
lightgbm>=4.0.0
xgboost>=2.0.0
scikit-learn>=1.4.0

# Deep Learning
torch>=2.2.0

# Reinforcement Learning
stable-baselines3>=2.3.0
gymnasium>=0.29.0
